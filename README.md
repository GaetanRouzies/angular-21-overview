# Angular 21 Overview

New features :

- Zoneless by default
- Generate AI rules files
- provideHttpClient() by default
- Regex in templates
- FormArrayDirectives
- SimpleChange Generic Type
- Signal Form Experimental
